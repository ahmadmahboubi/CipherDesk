namespace CipherDesk.App.Theming;

/// <summary>Which palette the application should use.</summary>
public enum ThemeMode
{
    /// <summary>Follow the Windows app theme, and react when the user changes it.</summary>
    System = 0,
    Light = 1,
    Dark = 2
}
